# Morgen's Plan — 24 maart 2026

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Drie dingen: (1) onafgerond werk van gisteren committen, (2) order printen direct na aanmaken, (3) orders doorzoekbaar maken op de lijstpagina.

**Architecture:**
- Print: server component redirect met `?print=1` param + client `AutoPrint` component dat `window.print()` triggert + `@media print` CSS om nav te verbergen.
- Zoeken: `getOrders()` uitbreiden met optionele `zoek` param, URL-gebaseerde filter op orders listpagina (server-side rendering, geen client state nodig).

**Tech Stack:** Next.js 15 App Router, Supabase, TypeScript, Tailwind CSS v4

---

## Taak 0: Onafgerond werk van gisteren afmaken en committen

**Context:** Er zijn uncommitted wijzigingen: `NavLink.tsx` (al klaar), `KlaarDownloadKnoppen.tsx` (al klaar), lege map `vrachten/[id]/klaar/`, en diverse gewijzigde bestanden (`layout.tsx`, `page.tsx`, `vrachten/[id]/page.tsx`, `StatusBadge.tsx`, actions-bestanden).

**Files:**
- Modify: `src/app/(app)/layout.tsx`
- Modify: `src/app/(app)/vrachten/[id]/page.tsx`
- Create: `src/app/(app)/vrachten/[id]/klaar/page.tsx`

- [ ] **Stap 1: Bekijk alle uncommitted wijzigingen**

```bash
cd "/Users/biko/Documents/New Order System"
git diff
git status
```

Check wat er in `layout.tsx`, `vrachten/[id]/page.tsx`, `StatusBadge.tsx` en de actions-bestanden is veranderd. Begrijp het geheel voordat je commit.

- [ ] **Stap 2: Bouw de klaar-pagina**

`src/app/(app)/vrachten/[id]/klaar/page.tsx` — wordt bereikt ná het aanmaken van een vracht-factuur. Toont de `KlaarDownloadKnoppen` component met vrachtbrief en factuur download.

```tsx
import { notFound } from 'next/navigation'
import { getVracht } from '@/lib/db/vrachten'
import { getFactuurVoorVracht } from '@/lib/db/facturen'
import { getLeveringenVoorVrachtFactuur } from '@/lib/db/facturen'
import { KlaarDownloadKnoppen } from '@/components/vrachten/KlaarDownloadKnoppen'
import Link from 'next/link'

export default async function VrachtKlaarPage({
  params,
}: {
  params: Promise<{ id: string }>
}) {
  const { id } = await params
  const vracht = await getVracht(id)
  if (!vracht) notFound()

  const factuur = await getFactuurVoorVracht(id)
  if (!factuur) notFound()

  const leveringen = await getLeveringenVoorVrachtFactuur(id)

  return (
    <div className="max-w-lg mx-auto mt-12">
      <div className="text-center mb-8">
        <div className="w-12 h-12 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
          <svg className="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M5 13l4 4L19 7" />
          </svg>
        </div>
        <h1 className="text-2xl font-bold mb-1">Vracht klaar</h1>
        <p className="text-gray-500 text-sm font-mono">{vracht.vrachtbrief_nummer}</p>
      </div>

      <KlaarDownloadKnoppen
        vracht={vracht}
        factuur={factuur}
        leveringen={leveringen}
        klantNaam={vracht.klant?.naam ?? ''}
      />

      <div className="mt-8 text-center">
        <Link href="/vrachten" className="text-sm text-gray-400 hover:text-gray-600">
          ← Terug naar vrachten
        </Link>
      </div>
    </div>
  )
}
```

Kijk in `src/lib/db/facturen.ts` en `src/lib/db/vrachten.ts` welke functies al bestaan (bijv. `getFactuurVoorVracht` of vergelijkbaar) en pas de imports aan.

- [ ] **Stap 3: Zorg dat `vrachten/[id]/page.tsx` naar klaar-pagina redirect na factuur aanmaken**

Controleer of de `VrachtFactuurKnop` of de server action al naar `/vrachten/${id}/klaar` redirect. Zo niet, pas de redirect aan in `src/lib/actions/vrachten.ts`:

```ts
// In createVrachtFactuur, verander de redirect naar:
redirect(`/vrachten/${vracht_id}/klaar`)
```

- [ ] **Stap 4: TypeScript check**

```bash
cd "/Users/biko/Documents/New Order System" && npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Stap 5: Commit alles**

```bash
cd "/Users/biko/Documents/New Order System"
git add src/components/ui/NavLink.tsx \
  src/components/vrachten/KlaarDownloadKnoppen.tsx \
  src/app/(app)/vrachten/[id]/klaar/page.tsx \
  src/app/(app)/layout.tsx \
  src/app/(app)/vrachten/[id]/page.tsx \
  src/app/(app)/orders/[id]/page.tsx \
  src/app/(app)/facturen/nieuw/page.tsx \
  src/app/(app)/facturen/page.tsx \
  src/app/page.tsx \
  src/components/orders/StatusBadge.tsx \
  src/lib/actions/leveringen.ts \
  src/lib/actions/orders.ts \
  src/lib/actions/vrachten.ts \
  src/lib/db/facturen.ts
git commit -m "feat: vracht klaar-pagina met download knoppen, NavLink component, nav refactor"
```

---

## Taak 1: Order printen na aanmaken

**Context:** Na het opslaan van een order wordt er nu genavigeerd naar `/orders/${id}`. We willen dat de gebruiker direct de order kan printen — zonder extra klikken. Aanpak: redirect naar `/orders/${id}?print=1`, en voeg een client component toe dat automatisch `window.print()` aanroept. Print-CSS verbergt de navigatie.

**Files:**
- Modify: `src/app/(app)/orders/nieuw/page.tsx` — redirect met `?print=1`
- Modify: `src/app/(app)/orders/[id]/page.tsx` — AutoPrint component invoegen
- Create: `src/components/orders/AutoPrint.tsx` — client component
- Modify: `src/app/globals.css` — `@media print` regels

- [ ] **Stap 1: Maak `src/components/orders/AutoPrint.tsx`**

```tsx
'use client'

import { useEffect } from 'react'

export function AutoPrint() {
  useEffect(() => {
    window.print()
  }, [])
  return null
}
```

- [ ] **Stap 2: Voeg print-CSS toe aan `src/app/globals.css`**

Voeg onderaan het bestand toe:

```css
@media print {
  /* Verberg nav en alles buiten de main content */
  nav,
  aside,
  .no-print {
    display: none !important;
  }

  body {
    background: white;
    color: black;
    font-size: 12pt;
  }

  a {
    color: inherit;
    text-decoration: none;
  }

  /* Voorkom dat tabel-rijen worden gesplitst over pagina's */
  tr {
    break-inside: avoid;
  }
}
```

- [ ] **Stap 3: Pas de redirect aan in `src/app/(app)/orders/nieuw/page.tsx`**

Zoek regel 41:
```tsx
redirect(`/orders/${order.id}`)
```
Vervang met:
```tsx
redirect(`/orders/${order.id}?print=1`)
```

- [ ] **Stap 4: Gebruik `AutoPrint` in `src/app/(app)/orders/[id]/page.tsx`**

Voeg import toe:
```tsx
import { AutoPrint } from '@/components/orders/AutoPrint'
```

Voeg het component toe aan de JSX, vlak na de openende `<div className="max-w-3xl">`:
```tsx
{searchParams.print === '1' && <AutoPrint />}
```

De page-functie moet ook `searchParams` ontvangen:
```tsx
export default async function OrderDetailPage({
  params,
  searchParams,
}: {
  params: Promise<{ id: string }>
  searchParams: Promise<{ print?: string }>
}) {
  const { id } = await params
  const { print } = await searchParams
  // ... rest van de functie
```

En in de JSX:
```tsx
{print === '1' && <AutoPrint />}
```

- [ ] **Stap 5: Voeg een "Print" knop toe op de order detail pagina**

Zoek de Bewerken-link in `orders/[id]/page.tsx`:
```tsx
<Link href={`/orders/${id}/bewerken`}
  className="text-sm border border-gray-300 px-3 py-1 rounded hover:bg-gray-50">
  Bewerken
</Link>
```

Vervang met (print-knop toegevoegd):
```tsx
<div className="flex gap-2">
  <button
    onClick={() => window.print()}
    className="no-print text-sm border border-gray-300 px-3 py-1 rounded hover:bg-gray-50"
  >
    Printen
  </button>
  <Link href={`/orders/${id}/bewerken`}
    className="text-sm border border-gray-300 px-3 py-1 rounded hover:bg-gray-50">
    Bewerken
  </Link>
</div>
```

Omdat de print-knop `onClick` gebruikt, moet de bestaande server component een kleine client wrapper worden, of je voegt de knop toe als aparte client component. Makkelijkste oplossing: maak `src/components/orders/PrintKnop.tsx`:

```tsx
'use client'

export function PrintKnop() {
  return (
    <button
      onClick={() => window.print()}
      className="no-print text-sm border border-gray-300 px-3 py-1 rounded hover:bg-gray-50"
    >
      Printen
    </button>
  )
}
```

En import + gebruik in `orders/[id]/page.tsx`:
```tsx
import { PrintKnop } from '@/components/orders/PrintKnop'
// ...
<div className="flex gap-2">
  <PrintKnop />
  <Link href={`/orders/${id}/bewerken`} ...>Bewerken</Link>
</div>
```

- [ ] **Stap 6: TypeScript check**

```bash
cd "/Users/biko/Documents/New Order System" && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Stap 7: Test handmatig**

1. Ga naar `/orders/nieuw`
2. Vul het formulier in en sla op
3. Print-dialoog zou automatisch moeten openen
4. Controleer dat nav niet zichtbaar is in de print-preview
5. Sluit dialoog — je bent nu op de order detail pagina
6. Druk op "Printen" knop — dialoog opent opnieuw

- [ ] **Stap 8: Commit**

```bash
cd "/Users/biko/Documents/New Order System"
git add src/components/orders/AutoPrint.tsx \
  src/components/orders/PrintKnop.tsx \
  src/app/(app)/orders/nieuw/page.tsx \
  src/app/(app)/orders/[id]/page.tsx \
  src/app/globals.css
git commit -m "feat: order print-dialoog na aanmaken, print-knop op detail pagina"
```

---

## Taak 2: Orders zoeken op de lijstpagina

**Context:** De orders pagina toont nu alleen paginering (50 per pagina). Er is al een `zoekOrdersVoorKloon()` functie in `db/orders.ts` voor de kloon-functie. We voegen zoekfunctionaliteit toe aan de hoofdpagina: URL-param `?zoek=...`, server-side filtering, zoekbalk in de header. Geen client state, werkt met normale navigatie.

**Files:**
- Modify: `src/lib/db/orders.ts` — `getOrders()` uitbreiden met `zoek` param
- Modify: `src/app/(app)/orders/page.tsx` — zoekbalk + zoek-param doorgeven
- Create: `src/components/orders/ZoekBalk.tsx` — client component voor zoekbalk

- [ ] **Stap 1: Schrijf de falende test (als er tests zijn voor db/orders)**

Controleer of `src/lib/db/orders.test.ts` bestaat:
```bash
ls "/Users/biko/Documents/New Order System/src/lib/db/"
```

Als er geen test-setup is voor DB-functies, sla tests over (Supabase queries zijn moeilijk te unit-testen zonder een echte DB). Ga door naar stap 2.

- [ ] **Stap 2: Pas `getOrders()` aan in `src/lib/db/orders.ts`**

Zoek de huidige signatuur:
```ts
export async function getOrders(page = 1, perPagina = 50): Promise<{ orders: Order[]; totaal: number }> {
```

Vervang de gehele functie:
```ts
export async function getOrders(
  page = 1,
  perPagina = 50,
  zoek?: string
): Promise<{ orders: Order[]; totaal: number }> {
  const supabase = await createClient()
  const van = (page - 1) * perPagina
  const tot = van + perPagina - 1

  let query = supabase
    .from('orders')
    .select('*, klant:klanten(id, naam), facturatie_code:facturatie_codes(id, code, tarief)', { count: 'exact' })
    .order('aangemaakt_op', { ascending: false })

  if (zoek && zoek.trim()) {
    query = query.or(`order_nummer.ilike.%${zoek}%,order_code.ilike.%${zoek}%`)
  }

  const { data, count, error } = await query.range(van, tot)
  if (error) throw error
  return { orders: data as Order[], totaal: count ?? 0 }
}
```

**Let op:** Zoeken op klantnaam via join (`.klant.naam`) is niet direct mogelijk met Supabase `.or()`. Als klantnaam-zoeken later nodig is, kan dat via een aparte query of een database view. Voor nu: order_nummer en order_code dekken de meeste use cases.

- [ ] **Stap 3: Maak `src/components/orders/ZoekBalk.tsx`**

```tsx
'use client'

import { useRouter, usePathname } from 'next/navigation'
import { useTransition } from 'react'

interface Props {
  defaultValue?: string
}

export function ZoekBalk({ defaultValue }: Props) {
  const router = useRouter()
  const pathname = usePathname()
  const [, startTransition] = useTransition()

  function handleChange(e: React.ChangeEvent<HTMLInputElement>) {
    const waarde = e.target.value
    startTransition(() => {
      if (waarde.trim()) {
        router.push(`${pathname}?zoek=${encodeURIComponent(waarde)}`)
      } else {
        router.push(pathname)
      }
    })
  }

  return (
    <input
      type="search"
      placeholder="Zoek op ordernummer of code..."
      defaultValue={defaultValue}
      onChange={handleChange}
      className="form-input w-64"
    />
  )
}
```

- [ ] **Stap 4: Pas `src/app/(app)/orders/page.tsx` aan**

Voeg `ZoekBalk` toe en geef `zoek` door aan `getOrders()`:

```tsx
import Link from 'next/link'
import { getOrders } from '@/lib/db/orders'
import { StatusBadge } from '@/components/orders/StatusBadge'
import { Pagination } from '@/components/ui/Pagination'
import { ZoekBalk } from '@/components/orders/ZoekBalk'
import { formatDate } from '@/lib/utils/formatters'

export default async function OrdersPage({
  searchParams,
}: {
  searchParams: Promise<{ pagina?: string; zoek?: string }>
}) {
  const params = await searchParams
  const pagina = parseInt(params.pagina ?? '1')
  const zoek = params.zoek ?? ''
  const perPagina = 50
  const { orders, totaal } = await getOrders(pagina, perPagina, zoek)
  const totaalPaginas = Math.ceil(totaal / perPagina)

  return (
    <div>
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-2xl font-bold">Orders</h1>
        <div className="flex items-center gap-3">
          <ZoekBalk defaultValue={zoek} />
          <Link href="/orders/nieuw" className="btn-primary">
            + Nieuwe order
          </Link>
        </div>
      </div>

      {zoek && (
        <p className="text-sm text-gray-500 mb-4">
          {totaal} {totaal === 1 ? 'resultaat' : 'resultaten'} voor &ldquo;{zoek}&rdquo;
        </p>
      )}

      <div className="bg-white rounded-lg border border-gray-200 overflow-hidden">
        <table className="w-full text-sm">
          <thead>
            <tr className="border-b border-gray-100 bg-gray-50/60">
              <th className="text-left px-4 py-3 font-medium text-gray-500 text-xs uppercase tracking-wide">Ordernummer</th>
              <th className="text-left px-4 py-3 font-medium text-gray-500 text-xs uppercase tracking-wide">Klant</th>
              <th className="text-left px-4 py-3 font-medium text-gray-500 text-xs uppercase tracking-wide">Code</th>
              <th className="text-right px-4 py-3 font-medium text-gray-500 text-xs uppercase tracking-wide">Grootte</th>
              <th className="text-left px-4 py-3 font-medium text-gray-500 text-xs uppercase tracking-wide">Status</th>
              <th className="text-left px-4 py-3 font-medium text-gray-500 text-xs uppercase tracking-wide">Datum</th>
            </tr>
          </thead>
          <tbody className="divide-y divide-gray-100">
            {orders.length === 0 && (
              <tr>
                <td colSpan={6} className="px-4 py-8 text-center text-gray-400 text-sm">
                  Geen orders gevonden{zoek ? ` voor "${zoek}"` : ''}.
                </td>
              </tr>
            )}
            {orders.map(order => (
              <tr key={order.id} className="hover:bg-gray-50/70 transition-colors">
                <td className="px-4 py-3">
                  <Link href={`/orders/${order.id}`} className="text-[#7C3AED] hover:text-[#6D28D9] font-mono text-xs font-medium">
                    {order.order_nummer}
                  </Link>
                </td>
                <td className="px-4 py-3 font-medium">{order.klant?.naam}</td>
                <td className="px-4 py-3 font-mono text-xs text-gray-400">{order.order_code}</td>
                <td className="px-4 py-3 text-right tabular-nums">{order.order_grootte.toLocaleString('nl-NL')}</td>
                <td className="px-4 py-3"><StatusBadge status={order.status} /></td>
                <td className="px-4 py-3 text-gray-400 text-xs">{formatDate(order.aangemaakt_op.split('T')[0])}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      <Pagination pagina={pagina} totaalPaginas={totaalPaginas} basisUrl={zoek ? `/orders?zoek=${encodeURIComponent(zoek)}` : '/orders'} />
    </div>
  )
}
```

**Let op de link-kleur:** De order-links waren blauw (`text-blue-600`), nu paars (`text-[#7C3AED]`) — consistent met de rest van het design system.

- [ ] **Stap 5: TypeScript check**

```bash
cd "/Users/biko/Documents/New Order System" && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Stap 6: Test handmatig**

1. Ga naar `/orders`
2. Zoek op een bestaand ordernummer (bijv. een deel ervan)
3. Resultaten filteren live terwijl je typt
4. Leeg maken → alle orders zichtbaar
5. Zoek op een niet-bestaande term → "Geen orders gevonden" melding

- [ ] **Stap 7: Commit**

```bash
cd "/Users/biko/Documents/New Order System"
git add src/lib/db/orders.ts \
  src/components/orders/ZoekBalk.tsx \
  src/app/(app)/orders/page.tsx
git commit -m "feat: zoekbalk op orders lijstpagina, filter op ordernummer en code"
```

---

## Klaar

Volgorde voor morgen:
1. **Taak 0** — uncommitted werk van gisteren reviewen en committen
2. **Taak 1** — order printen (snel, ~30 min)
3. **Taak 2** — orders zoeken (~45 min)

Daarna: kijk wat er nog schort aan de flow en beslis of er verdere polish nodig is.
